export enum ModalNotifications {
  ALREADY_COMPARED_BRANCHES = 'You\'re already comparing these branches',
  COMPARE_BRANCH_WITH_ITSELF = 'You can\'t compare branches with itself',
  COMPARE_DEFAULT_BRANCHES = 'Default branches are already compared'
}
